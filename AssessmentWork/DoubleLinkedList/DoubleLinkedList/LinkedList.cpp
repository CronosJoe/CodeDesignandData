#include <iostream>
#include "LinkedList.h"
using namespace std;
int main()
{
	//program to show linked list understanding
	std::cout << "Hey this is my double linked list portion of the assessment!" << std::endl;
	//setup
	//tList test; //I can't seem to initialize a class object anymore... I need to go to bed for now this is what I'll submit I need the exes for this, hashing, and binary trees 
	bool finished = false;
	while (!finished)
	{
		
		std::cout << "" << std::endl;
		//accept input here

		//once input has been accepted check it here, then run the corresponding method, if command quit or q is given change finished to true and end the program by calling deconstructor
	}
	return 0;
}