#include "LinkedList.h"

int main()
{
	/*//test nodes
	Node* head = NULL;
	Node* second = NULL;
	Node* third = NULL;

	head = new Node();
	second = new Node();
	third = new Node();

	//setting the data for each of the test nodes, the basics will be handled in a constructor later
	head->data = 1;
	head->next = second;
	head->prev = NULL;

	second->data = 2;
	second->next = third;
	second->prev = head;

	third->data = 3;
	third->next = NULL;
	third->prev = second;
	
	//function testing
	*/
	return 0;
}